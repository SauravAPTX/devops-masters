# This file will be generated by kubeseal command in GitHub Actions
# It contains the encrypted version of your secrets
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: app-secrets
  namespace: default
spec:
  encryptedData:
    database-url: # Encrypted value will be here
    api-key: # Encrypted value will be here
  template:
    metadata:
      name: app-secrets
      namespace: default
